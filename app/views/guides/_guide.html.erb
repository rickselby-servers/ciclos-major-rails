<div id="<%= dom_id guide %>" class="clearfix">
  <img class="float-end ms-3 mb-3">

  <h2><%= guide.name %></h2>

  <%= sanitize guide.description %>

  <% if admin_signed_in? %>
    <div>
      <%= link_to edit_guide_path(guide), class: %i[btn btn-secondary] do %>
        <%= fa_icon "pencil" %>
        <%= t ".edit" %>
      <% end %>
    </div>
  <% end %>
</div>
